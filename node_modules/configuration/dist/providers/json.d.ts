import { Data, DataRaw, DataUpdate, ProviderJSONOptions } from '../types';
import ProviderFile from './file';
declare class ProviderJSON<Options extends ProviderJSONOptions = ProviderJSONOptions> extends ProviderFile<Options> {
    read(): Promise<DataUpdate>;
    readSync(): DataUpdate;
    write(data: Data | DataRaw, force?: boolean): Promise<void>;
    writeSync(data: Data | DataRaw, force?: boolean): void;
}
export default ProviderJSON;
