import { ProviderStorageOptions } from '../types';
import ProviderStorage from './storage';
declare class ProviderSessionStorage<Options extends ProviderStorageOptions = ProviderStorageOptions> extends ProviderStorage<Options> {
    constructor(options?: Partial<Options>);
}
export default ProviderSessionStorage;
