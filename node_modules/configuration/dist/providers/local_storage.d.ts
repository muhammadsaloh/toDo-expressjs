import { ProviderStorageOptions } from '../types';
import ProviderStorage from './storage';
declare class ProviderLocalStorage<Options extends ProviderStorageOptions = ProviderStorageOptions> extends ProviderStorage<Options> {
    constructor(options?: Partial<Options>);
}
export default ProviderLocalStorage;
