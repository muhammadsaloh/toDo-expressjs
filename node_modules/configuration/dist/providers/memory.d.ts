import { Data, DataRaw, DataUpdate, ProviderMemoryOptions } from '../types';
import ProviderAbstract from './abstract';
declare class ProviderMemory<Options extends ProviderMemoryOptions = ProviderMemoryOptions> extends ProviderAbstract<Options> {
    read(): Promise<DataUpdate>;
    readSync(): DataUpdate;
    write(data: Data | DataRaw, force?: boolean): Promise<void>;
    writeSync(data: Data | DataRaw, force?: boolean): void;
}
export default ProviderMemory;
