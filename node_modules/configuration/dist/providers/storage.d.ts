import { Data, DataRaw, DataUpdate, ProviderStorageOptions } from '../types';
import ProviderMemory from './memory';
declare class ProviderStorage<Options extends ProviderStorageOptions = ProviderStorageOptions> extends ProviderMemory<Options> {
    id: string;
    storage?: Storage;
    constructor(options: Partial<Options>);
    readSync(): DataUpdate;
    writeSync(data: Data | DataRaw, force?: boolean): void;
}
export default ProviderStorage;
