import { Callback, Options, Task } from './types';
declare class Limiter {
    concurrency: number;
    count: number;
    queue: Set<Callback>;
    constructor(options: Options);
    add<T>(fn: Task<T>): Promise<T>;
    flush(): void;
    run<T>(fn: Task<T>): Promise<T>;
}
declare const _default: typeof Limiter & {
    default: typeof Limiter;
}
declare namespace _default {
    export type type = Limiter;
}
export = _default;
